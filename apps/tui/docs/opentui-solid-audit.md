# X1Shell OpenTUI Solid Audit

Date: 2026-05-20

Scope: `apps/tui` was checked against the sibling OpenCode checkout at `/home/xjin/Program/dev/my-agents-platform/opencode/packages/opencode/src/cli/cmd/tui`, OpenTUI Solid docs, and the local OpenTUI implementation in `/home/xjin/Program/dev/my-agents-platform/opentui/packages/solid` and `/home/xjin/Program/dev/my-agents-platform/opentui/packages/core`.

| Area                       | OpenCode / OpenTUI pattern                                                                                                     | Current X1Shell implementation                                                                                       | Status                | Risk   | Fix                                                           |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- | --------------------- | ------ | ------------------------------------------------------------- |
| Solid package wiring       | `@opentui/solid` render API, JSX import source, Bun preload                                                                    | `apps/tui` uses `@opentui/solid`, `jsxImportSource`, and Bun preload                                                 | Aligned               | Low    | None                                                          |
| React migration leftovers  | No `@opentui/react`, React hooks, or React test utilities in runtime                                                           | Search found no runtime React/OpenTUI React leftovers in `apps/tui`                                                  | Aligned               | Low    | None                                                          |
| Renderer config            | OpenCode creates `CliRenderer` explicitly with `exitOnCtrlC: false`, disabled console error overlay, controlled mouse/keyboard | X1Shell uses explicit renderer, disabled console, manual Ctrl+C/exit handling, optional Kitty keyboard               | Acceptable divergence | Low    | Keep product-specific keyboard policy                         |
| Renderer lifecycle         | Solid root is disposed by renderer destroy; process listeners cleaned on destroy                                               | X1Shell destroys renderer and removes process handlers, but interactive shutdown had an obsolete empty unmount block | Misaligned            | Low    | Remove stale unmount path in cleanup pass                     |
| Test renderer lifecycle    | OpenTUI tests destroy the renderer after `testRender`                                                                          | X1Shell smoke scripts destroy renderers in `finally`                                                                 | Aligned               | Low    | None                                                          |
| Global keyboard routing    | OpenCode uses keymap layers and scoped command routing                                                                         | X1Shell uses direct `useKeyboard` routing because product command surface is smaller                                 | Acceptable divergence | Medium | Keep direct routing, but verify stale closure and paste paths |
| Paste handling             | OpenTUI Solid exposes `usePaste`; OpenCode prompt/components handle editor/paste behavior through OpenTUI input primitives     | X1Shell previously handled only keypress `sequence`; bracketed paste could bypass composer/palette/attachment flow   | Misaligned            | Medium | Added `usePaste` routing and headless smoke                   |
| Composer prompt            | OpenCode relies on focusable textarea/editor primitives with managed input bindings                                            | X1Shell renders OpenTUI `<textarea>` but state is controlled by app-level routing                                    | Acceptable divergence | Medium | Preserve semantics; paste fix closes main input gap           |
| Store subscription cleanup | Solid components should unsubscribe in `onCleanup`                                                                             | `TuiRuntimeApp` unsubscribes all stores in `onCleanup`                                                               | Aligned               | Low    | None                                                          |
| Async request staleness    | OpenCode sync ignores stale events through state/session identity                                                              | X1Shell diff and filesystem browse requests use request IDs/thread guards                                            | Aligned               | Low    | None                                                          |
| Markdown/text safety       | OpenCode uses OpenTUI markdown/code renderables with themed output; unsafe control text must not reach terminal                | X1Shell has `SafeMarkdown`, `displayText`, and terminal-control stripping                                            | Aligned               | Medium | Keep smoke coverage                                           |
| Diff rendering             | OpenCode uses native `<diff>` inside scrollable regions                                                                        | X1Shell uses native `<diff>` via `DiffPanel` with sanitized diff text                                                | Aligned               | Low    | None                                                          |
| Resize responsiveness      | OpenCode uses `useTerminalDimensions`                                                                                          | X1Shell uses `useTerminalDimensions` and responsive layout helpers                                                   | Aligned               | Low    | Keep headless size coverage                                   |
| Dialog/palette             | OpenCode has provider-based dialogs and command palette                                                                        | X1Shell uses a single product-specific `CommandPalette` overlay                                                      | Acceptable divergence | Low    | Keep simpler model                                            |
| Headless smoke             | OpenCode tests use OpenTUI test fixtures and captures                                                                          | X1Shell has headless app and component smoke scripts                                                                 | Aligned               | Medium | Added paste smoke coverage                                    |

Follow-up audit passes should append rows here for each additional finding before editing.
